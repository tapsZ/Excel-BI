source "https://rubygems.org"

# Matches the exact gem versions GitHub Pages runs, so a local build is a
# faithful preview of the published site.
gem "github-pages", group: :jekyll_plugins

gem "webrick", "~> 1.8"
